services:
  - type: web
    name: knowledge
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn knowledge.main:app --worker-class uvicorn.workers.UvicornWorker --workers 1 --bind 0.0.0.0:$PORT --timeout 120 --log-level info
