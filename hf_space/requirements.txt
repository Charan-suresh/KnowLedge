fastapi==0.112.2
uvicorn==0.30.6
# gradio is injected automatically by HF Spaces as gradio[oauth]==4.44.1 -- do not pin here

# transformers 5.x required: unsloth/gemma-4-E4B-it uses Gemma4ForConditionalGeneration
# which was introduced in transformers 5.0+ (config.json shows transformers_version 5.6.0.dev0)
transformers>=5.0.0
# torch>=2.4.0 required by transformers 5.x (it disables PyTorch if version < 2.4)
torch>=2.4.0
torchvision  # required for Gemma 4 vision / image processing
accelerate>=0.30.0

# Utilities
Pillow
# huggingface_hub>=1.3.0 required by transformers 5.x
huggingface_hub>=1.3.0
